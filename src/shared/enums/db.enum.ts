export enum Status {
  Published = 'published',
  Draft = 'draft',
  Archived = 'archived',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
}

export enum Role {
  Member = 'member',
  Admin = 'admin',
  Owner = 'owner',
}
